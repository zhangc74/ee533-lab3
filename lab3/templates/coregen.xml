<?xml version="1.0" encoding="UTF-8"?>
<RootFolder label="COREGEN" treetype="folder" language="COREGEN">
	<Folder label="VERILOG Component Instantiation" treetype="folder">
		<Template label="dualportmemory" treetype="template">
 
 
// The following must be inserted into your Verilog file for this
// core to be instantiated. Change the instance name and port connections
// (in parentheses) to your own signal names.
 
dualportmemory YourInstanceName (
    .addra(addra), // Bus [7 : 0] 
    .addrb(addrb), // Bus [7 : 0] 
    .clka(clka),
    .clkb(clkb),
    .dina(dina), // Bus [71 : 0] 
    .dinb(dinb), // Bus [71 : 0] 
    .douta(douta), // Bus [71 : 0] 
    .doutb(doutb), // Bus [71 : 0] 
    .wea(wea),
    .web(web));

 
		</Template>
		<Template label="dual_core_memory_element" treetype="template">
 
 
// The following must be inserted into your Verilog file for this
// core to be instantiated. Change the instance name and port connections
// (in parentheses) to your own signal names.
 
dual_core_memory_element YourInstanceName (
    .addra(addra), // Bus [7 : 0] 
    .addrb(addrb), // Bus [7 : 0] 
    .clka(clka),
    .clkb(clkb),
    .dina(dina), // Bus [71 : 0] 
    .dinb(dinb), // Bus [71 : 0] 
    .douta(douta), // Bus [71 : 0] 
    .doutb(doutb), // Bus [71 : 0] 
    .wea(wea),
    .web(web));

 
		</Template>
	</Folder>
	<Folder label="VHDL Component Instantiation" treetype="folder">
		<Template label="dualportmemory" treetype="template">
 
 
-- The following code must appear in the VHDL architecture header:
 
component dualportmemory
    port (
    addra: IN std_logic_VECTOR(7 downto 0);
    addrb: IN std_logic_VECTOR(7 downto 0);
    clka: IN std_logic;
    clkb: IN std_logic;
    dina: IN std_logic_VECTOR(71 downto 0);
    dinb: IN std_logic_VECTOR(71 downto 0);
    douta: OUT std_logic_VECTOR(71 downto 0);
    doutb: OUT std_logic_VECTOR(71 downto 0);
    wea: IN std_logic;
    web: IN std_logic);
end component;


 
-------------------------------------------------------------
 
-- The following code must appear in the VHDL architecture body.
-- Substitute your own instance name and net names.
 
your_instance_name : dualportmemory
        port map (
            addra =&gt; addra,
            addrb =&gt; addrb,
            clka =&gt; clka,
            clkb =&gt; clkb,
            dina =&gt; dina,
            dinb =&gt; dinb,
            douta =&gt; douta,
            doutb =&gt; doutb,
            wea =&gt; wea,
            web =&gt; web);
 
		</Template>
		<Template label="dual_core_memory_element" treetype="template">
 
 
-- The following code must appear in the VHDL architecture header:
 
component dual_core_memory_element
    port (
    addra: IN std_logic_VECTOR(7 downto 0);
    addrb: IN std_logic_VECTOR(7 downto 0);
    clka: IN std_logic;
    clkb: IN std_logic;
    dina: IN std_logic_VECTOR(71 downto 0);
    dinb: IN std_logic_VECTOR(71 downto 0);
    douta: OUT std_logic_VECTOR(71 downto 0);
    doutb: OUT std_logic_VECTOR(71 downto 0);
    wea: IN std_logic;
    web: IN std_logic);
end component;


 
-------------------------------------------------------------
 
-- The following code must appear in the VHDL architecture body.
-- Substitute your own instance name and net names.
 
your_instance_name : dual_core_memory_element
        port map (
            addra =&gt; addra,
            addrb =&gt; addrb,
            clka =&gt; clka,
            clkb =&gt; clkb,
            dina =&gt; dina,
            dinb =&gt; dinb,
            douta =&gt; douta,
            doutb =&gt; doutb,
            wea =&gt; wea,
            web =&gt; web);
 
		</Template>
	</Folder>
</RootFolder>
